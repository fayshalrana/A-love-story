# Site URL for Open Graph metadata (set to your Vercel domain in production)
NEXT_PUBLIC_SITE_URL=https://a-love-story-eta.vercel.app

# Supabase (optional — for guest wishes persistence)
# Create a table: guest_wishes (id, name, relationship, message, approved, created_at)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
